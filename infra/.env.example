# Copy this file to infra/.env and fill in a real value.
# infra/.env is gitignored (see .gitignore's "Secrets - NEVER COMMIT" section) —
# never commit the actual password.
POSTGRES_PASSWORD=changeme
